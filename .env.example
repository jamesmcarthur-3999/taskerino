# Taskerino Environment Variables Template
# Copy this file to .env for local development
# NEVER commit .env files with actual credentials

# Claude API Key (required for AI features)
# Get your API key from https://console.anthropic.com/
# VITE_CLAUDE_API_KEY=sk-ant-...

# OpenAI API Key (optional - for audio transcription)
# Get your API key from https://platform.openai.com/
# VITE_OPENAI_API_KEY=sk-...

# Build Configuration
NODE_ENV=production

# Application Version
# This should match package.json version
VITE_APP_VERSION=0.1.0

# Feature Flags (optional)
# VITE_ENABLE_BETA_FEATURES=false
# VITE_ENABLE_ANALYTICS=false

# Note: API keys are now stored securely in Tauri's secure storage
# These environment variables are only needed for development/testing
